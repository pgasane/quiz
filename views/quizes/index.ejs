<form name="busqueda" id="busqueda" action="/quizes">
	<input type="text" name="search" id="search"
		   value="<%=query%>" placeholder="Indroduzca su búsqueda"	/>
	
	<label for="search">Buscar preguntas</label>
	
	<input type="submit" value="Buscar"	/>
	
	<%if(query) { %> <a href="/quizes">Anular el filtro</a> <% }; %>
</form>

<table>
<% var i; for (i=0; i < quizes.length; i++) { %>
	<tr><td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td></tr>
<% } %>
</table>